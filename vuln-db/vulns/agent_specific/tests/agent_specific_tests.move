/// Package smoke test — intentionally minimal.
/// See individual module test files for comprehensive coverage.
#[test_only]
module agent_specific::agent_specific_tests {
    #[test]
    fun test_package_load() {
        // Package compiles → passes
    }
}
